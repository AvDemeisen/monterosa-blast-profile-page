import styled from 'styled-components';

export const Wrapper = styled.div`
    max-width: 460px;
    min-height: 100vh;
    margin: 0px auto;
    text-align: center;
    display: grid;
    grid-template-rows: auto 1fr;
`;